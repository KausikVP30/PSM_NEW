[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kaggle-rag-v1"
version = "0.1.0"
description = "Lightweight confidence-gated RAG for Kaggle"
